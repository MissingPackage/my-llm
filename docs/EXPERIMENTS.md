# Experiment log

Copy this section for every significant run.

## RUN-ID

- Git commit:
- date/time and duration:
- laptop, TGP, driver, PyTorch/CUDA:
- config:
- seed:
- tokenizer SHA-256:
- manifest and dataset revision:
- initial checkpoint:
- base model and adapter/merge lineage:
- quantization, LoRA rank/alpha/target and LoftQ/DoRA:
- attention backend, Liger, packing/offload/compile:
- parameter/token budget:
- median token/s:
- peak VRAM and temperature:
- train/validation loss:
- reward mean/std, frac zero-std, DPO margin/accuracy:
- benchmark before/after:
- qualitative samples:
- anomalies, OOM, resume:
- decision: continue / discard / ablation:
